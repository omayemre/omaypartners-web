# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# local Claude Code settings (contains WP application-password entries
# used during content migration - never commit)
.claude/

# personal working notes / internal audit docs - not site source, not for
# the public repo
.00 Emre Notlar/
audit/
docs/

# owner's source spreadsheets/documents dropped into the project folder for
# me to read (e.g. the project-list master Excel) - the data gets absorbed
# into src/data/*.json, the raw file itself isn't site source
*.xlsx
~$*.xlsx
